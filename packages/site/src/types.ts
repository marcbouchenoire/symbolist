export interface Data {
  /**
   * The current year.
   */
  date: string

  /**
   * The latest package version.
   */
  version: string
}

export interface NamedSymbol {
  /**
   * The symbol's name.
   */
  name: string

  /**
   * The symbol's character.
   */
  symbol: string
}
