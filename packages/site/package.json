{
  "name": "site",
  "version": "1.1.0",
  "private": true,
  "lint-staged": {
    "**/*.mjs": [
      "eslint --fix",
      "prettier --write"
    ],
    "**/*.{ts,tsx}": [
      "eslint --fix",
      "tsatsiki --project tsconfig.json next-env.d.ts",
      "prettier --write"
    ],
    "**/*.scss": [
      "stylelint --fix",
      "prettier --write"
    ],
    "**/*.{json,md,yml}": [
      "prettier --ignore-path .eslintignore --write"
    ]
  },
  "scripts": {
    "precommit": "lint-staged",
    "dev": "next dev",
    "build": "next build",
    "lint": "yarn lint:eslint & yarn lint:stylelint && yarn lint:tsc && yarn lint:prettier",
    "lint:eslint": "eslint '**/*.{mjs,ts,tsx}' --fix",
    "lint:prettier": "prettier --write '**/*.{mjs,ts,tsx,json,md,yml,scss}' --loglevel silent",
    "lint:stylelint": "stylelint '**/*.scss' --fix",
    "lint:tsc": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "clsx": "^1.1.1",
    "framer-motion": "^5.3.2",
    "symbolist": "^1.1.0",
    "just-debounce-it": "^3.0.1",
    "leva": "^0.9.15",
    "next": "^12.0.4",
    "next-seo": "^4.28.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@bouchenoiremarc/stylelint-config": "^1.2.0",
    "@types/css-font-loading-module": "^0.0.7",
    "@types/react": "^17.0.36",
    "@types/react-dom": "^17.0.11",
    "sass": "^1.43.4",
    "stylelint": "^14.1.0"
  }
}
